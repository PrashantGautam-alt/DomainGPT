# Secrets — never commit real keys, only .env.example
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Scraped/derived corpus content and build artifacts — regenerate via src/ingest.py,
# don't redistribute scraped third-party text (NCFE/SEBI/Wikipedia) verbatim in a public repo.
# Fine-tuned weights ship to HuggingFace Hub, not git.
data/raw/
data/processed/
embeddings/
models/

# Eval artifacts derived from the corpus — review before ever un-ignoring
eval/eval_set.json

# OS
.DS_Store
