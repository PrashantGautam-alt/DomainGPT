# Live-app deps ONLY (Streamlit Community Cloud reads this). Lightweight — the LLM is a
# Groq API call, so no torch-training stack. sentence-transformers pulls a CPU torch for
# the embedding model. Training deps are in requirements-train.txt (server only).
streamlit
faiss-cpu
sentence-transformers
openai
python-dotenv
huggingface_hub
numpy
